<UserControl x:Class="Surf_Clean.Views.WebBrowserView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Surf_Clean.Surf_Clean.Views"
             xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
             xmlns:viewmodels="clr-namespace:Surf_Clean.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Navigation Bar -->
        <Grid Grid.Row="0" Background="White" Margin="0,0,0,1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Navigation Controls -->
            <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="5">
                <Button Command="{Binding GoBackCommand}" ToolTip="Go Back" Width="32" Height="32" Margin="2,0" Background="#E0F7FA" Foreground="#006064" BorderThickness="0">
                    <TextBlock Text="←" FontSize="14"/>
                </Button>
                <Button Command="{Binding GoForwardCommand}" ToolTip="Go Forward" Width="32" Height="32" Margin="2,0" Background="#E0F7FA" Foreground="#006064" BorderThickness="0">
                    <TextBlock Text="→" FontSize="14"/>
                </Button>
                <Button Command="{Binding RefreshCommand}" ToolTip="Refresh" Width="32" Height="32" Margin="2,0" Background="#E0F7FA" Foreground="#006064" BorderThickness="0">
                    <TextBlock Text="↻" FontSize="14"/>
                </Button>
                <Button Command="{Binding HomeCommand}" ToolTip="Home" Width="32" Height="32" Margin="2,0" Background="#E0F7FA" Foreground="#006064" BorderThickness="0">
                    <TextBlock Text="🏠" FontSize="14"/>
                </Button>
            </StackPanel>

            <!-- Address Bar -->
            <Border Grid.Column="1" 
                    Background="#F5F5F5" 
                    CornerRadius="18" 
                    Padding="5"
                    Margin="5,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Secure indicator -->
                    <TextBlock Grid.Column="0" 
                              Text="🌊" 
                              VerticalAlignment="Center" 
                              Margin="5,0"
                              Foreground="#00BCD4"/>

                    <!-- URL TextBox -->
                    <TextBox Grid.Column="1" 
                             Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" 
                             BorderThickness="0" 
                             Background="Transparent"
                             Padding="5,0"
                             KeyDown="UrlTextBox_KeyDown"
                             VerticalContentAlignment="Center"
                             Foreground="#212121"/>

                    <!-- Search Button -->
                    <Button Grid.Column="2" 
                            Command="{Binding NavigateCommand}" 
                            Content="Surf" 
                            Width="40" 
                            Margin="5,0"
                            Background="#00BCD4"
                            Foreground="White"
                            BorderThickness="0"/>
                </Grid>
            </Border>

            <!-- Privacy Controls -->
            <StackPanel Grid.Column="2" Orientation="Horizontal" Margin="5">
                <!-- Privacy Shield Button -->
                <Button Width="32" Height="32" Margin="2,0" ToolTip="Surf Clean Protection Status" Background="#00BCD4" BorderThickness="0">
                    <Grid>
                        <TextBlock Text="🌊" FontSize="16"/>
                        <TextBlock Text="{Binding TrackersBlocked}" FontSize="10" 
                                  FontWeight="Bold" HorizontalAlignment="Center" 
                                  VerticalAlignment="Center" Foreground="White" Margin="0,2,0,0"/>
                    </Grid>
                </Button>

                <!-- Settings Button -->
                <Button Width="32" Height="32" Margin="2,0" Background="#E0F7FA" BorderThickness="0">
                    <TextBlock Text="⚙️" FontSize="16"/>
                </Button>
            </StackPanel>
        </Grid>

        <!-- WebView2 Browser Control -->
        <wv2:WebView2 Grid.Row="1" x:Name="webView" Source="https://duckduckgo.com" AllowExternalDrop="False"/>

        <!-- Status Bar -->
        <Grid Grid.Row="2" Background="#E0F7FA">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Privacy Status -->
            <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="10,5">
                <StackPanel Orientation="Horizontal" Margin="0,0,10,0">
                    <CheckBox IsChecked="{Binding TrackingProtectionEnabled}" Content="WaveWash" VerticalAlignment="Center" Foreground="#006064"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,0,10,0">
                    <CheckBox IsChecked="{Binding CookieBlockingEnabled}" Content="SandScrubber" VerticalAlignment="Center" Foreground="#006064"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <CheckBox IsChecked="{Binding FingerprintProtectionEnabled}" Content="CoralGuard" VerticalAlignment="Center" Foreground="#006064"/>
                </StackPanel>
            </StackPanel>

            <!-- Tracker Counter -->
            <TextBlock Grid.Column="1" Margin="10,5" VerticalAlignment="Center" Foreground="#006064">
                <Run Text="Trackers Washed Away: "/>
                <Run Text="{Binding TrackersBlocked}" FontWeight="Bold"/>
            </TextBlock>
        </Grid>
    </Grid>
</UserControl>
